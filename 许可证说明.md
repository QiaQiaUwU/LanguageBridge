# 许可证与来源说明

## 本项目采用 GPL-3.0

**注意：源码包里没有 `LICENSE` 文件**，README 里的链接在本地是坏的。
上传 GitHub 时补一下即可（GPL 要求随附许可证全文，这不是可选项）。

补的办法（三选一，第一种最省事）：

- **新建仓库时**：Add a license 下拉里选 **GNU General Public License v3.0**，
  仓库建好就自带 `LICENSE`，README 里的链接自动就通了

- GitHub 网页上：仓库 → Add file → Create new file → 文件名填 `LICENSE`
  → 右侧出现 "Choose a license template" → 选 **GNU General Public License v3.0** → 提交
- 或从 https://www.gnu.org/licenses/gpl-3.0.txt 下载全文存成 `LICENSE`

`package.json` 的 `license` 字段已经写成 `GPL-3.0-or-later`。

## 为什么只能是 GPL-3.0

项目里有两个文件是从 **typing-word（GPL-3.0）** 移植的，文件头都写明了来源：

| 文件 | 移植自 |
| --- | --- |
| `shared/core/wordLookup.ts` | `packages/core/src/hooks/useWordLookup.ts` 等 |
| `shared/core/keySound.ts` | `packages/core/src/hooks/sound.ts` 等 |

另有几处**参照其行为实现**（不是复制代码），也已在注释里注明：

- `shared/core/typeStep.ts` —— 打字判定逻辑对照它的行为写的
- `shared/core/studySettings.ts` —— 沿用了它的哨兵值和音色选项名
- `shared/core/textSplit.ts` —— 断句时参考了它对编号标题的处理

GPL-3.0 是**传染性**许可证：衍生作品必须以同样的许可证发布。
所以这个项目**不能改成 MIT**，也不能闭源分发。这一点在决定怎么发布之前要想清楚。

## 其他依赖

- **FSRS** —— 记忆调度算法
- **3d-force-graph** —— 3D 关系图
- **onnxruntime-web** —— 本机 wav2vec2 推理（音频对轴）
- **transformers.js** —— 本机 Whisper 转写（跟读识别，从 CDN 加载）
- 词库释义数据与配色来自作者自己的 VocabVerse 项目

这些的许可证各自宽松（MIT/Apache 类），不影响上面的结论。
