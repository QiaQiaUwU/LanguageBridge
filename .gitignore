# ============================================================
# LanguageBridge .gitignore
#
# 分三类，判据是"能不能重新生成"和"是不是你个人的东西"：
#   1. 能重新生成的 —— 依赖、构建产物、缓存。留在仓库里只会让 clone 变慢，
#      而且它们跟机器强相关，别人拉下来也用不了。
#   2. 你个人的数据 —— 词库、学习记录、文章、设置。这是自己的资料，
#      不该跟代码一起公开；别人 clone 之后从空库开始、自己导入。
#   3. 会泄密的 —— API Key 之类。这个项目把 key 存在浏览器 localStorage 里，
#      源码没有硬编码（已扫过），所以这一类只需防住误提交的本地配置文件。
# ============================================================

# ---- 依赖与构建产物 ----
node_modules/
dist/
# 两条打包路线的产物：pkg 走 dist-exe，electron-builder 走 dist-desktop
dist-exe/
dist-desktop/
*.log

# 依赖/构建的哈希标记：server.mjs 靠它判断要不要重装重建，跟机器绑定
.lb-deps-hash
.lb-build-hash

# ---- 个人数据（重点）----
# 应用状态：词条工作缓存、词表、记忆卡片、活动日志、设置
data/

# 学习资源：一万五千个词条 JSON、收藏的文章。体积很大，而且是自己的资料
resources/

# 早期版本的目录名，老装机可能还留着
vendor-data/
public/data/word_explanations/

# 释义库搬家时留下的备份目录
.old-word_explanations-*/

# ---- 运行时杂项 ----
.app-window/
port.txt

# ---- 只给自己看的开发笔记 ----
# 这几份是过程记录和演示话术：写了大量"这里还没做""那个是死代码"，
# 是给自己排期用的，公开仓库里放着既没用也容易误导人。
# 想留档就存在本地，不进仓库。
待办与方案.md
展示稿.md
上传到GitHub.md
docs/项目现状说明.md
docs/GitHub发布清单.md
docs/介绍视频稿.md
docs/README-旧版备份.md
# 需求文档同样是进度记录，跟上面几份一个性质
docs/需求文档-v2.md

# ---- 编辑器 / 系统 ----
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# ---- 万一有人把密钥写进本地配置 ----
.env
.env.local
*.local.json
